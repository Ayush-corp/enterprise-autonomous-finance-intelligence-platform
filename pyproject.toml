[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autonomous-investing-agent"
version = "0.1.0"

[tool.pytest.ini_options]
asyncio_mode = "strict"
testpaths = ["tests"]
